# Static site - no build step
[build]
  publish = "."

# Node version: also set via .nvmrc (Netlify uses whichever is present)
# See https://docs.netlify.com/build/configure-builds/manage-dependencies/
[build.environment]
  NODE_VERSION = "20"
